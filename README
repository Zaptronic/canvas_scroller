I hope that this will become a parallax content presentation device, but for now it is just an image slider using the canvas drawing context and jquery. Will add features periodically to make it easier to use for the developer and end-user.